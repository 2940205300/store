登录时前端，服务端双重加密,前端加盐(密码)，服务端加盐(3344),
账号或密码输入错误时，服务端找不到，返回catch(code==500,防止状态码错误,服务返回status=201),
前端判断res==undefined


修改商品信息时:出版时间默认为当前时间;重新上传图片后，原来的图片将被删除;不上传时，保存原来的图片(基本上都是修改的服务端)

bug:新注册的用户没有地址也会提交到订单